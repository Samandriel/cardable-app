import 'package:flutter/material.dart';

class FlashcardItem extends StatelessWidget {
  const FlashcardItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
